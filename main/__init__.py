"""Make `main` importable as a package so `python -m main.train` works.

This file is intentionally empty; its presence lets Python treat the `main`
directory as a package when running modules with `-m`.
"""
